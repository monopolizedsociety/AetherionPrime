// Placeholder entry. Replace with Vite scaffolding.
// Example integration points:
// - Initialize MetaMask Embedded Wallet
// - Render onboarding steps
export default function App() {
  return (
    <div style={{fontFamily:'sans-serif', padding:20}}>
      <h1>AetherionGenesis</h1>
      <p>Seedless onboarding across Solana + EVM with an AI concierge.</p>
    </div>
  );
}
